import React from 'react';
import styled from 'styled-components';

const Work = () => (
    <Section>
    </Section>
 );

 export default Work;

 const Section = styled.section`
    padding: 20px;
`;